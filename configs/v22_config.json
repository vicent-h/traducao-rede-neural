{
    "invert_src": false,
    "embedding_dim": 512,
    "encoder_hidden_dim": 1024,
    "decoder_hidden_dim": 1024,
    "encoder_num_layers": 2,
    "decoder_num_layers": 2,
    "encoder_dropout": 0.4,
    "decoder_dropout": 0.4,
    "encoder_bidirectional": true,
    "batch_size": 128,
    "accum_steps": 1,
    "log_steps": 500,
    "save_steps": 1000,
    "eval_steps": 1000,
    "device": "cuda",
    "desc": "Diminuindo a quantidade de layers, aumentando os hidden sizes e embed dims e aumentando o learning rate",
    "name": "v22",
    "early_stop_patience": 20,
    "early_stop_min_delta": 0.0,
    "warmup_steps": 25000,
    "max_steps": 200000,
    "learning_rate": 0.0001
}